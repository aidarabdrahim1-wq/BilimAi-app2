
{
  "entities": {
    "StudentProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StudentProfile",
      "type": "object",
      "description": "Represents a student's personal learning profile, including their settings, progress, and preferences.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the StudentProfile entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the student."
        },
        "className": {
          "type": "string",
          "description": "The student's current academic class or grade level."
        },
        "currentScore": {
          "type": "number",
          "description": "The student's current overall score or performance metric."
        },
        "targetScore": {
          "type": "number",
          "description": "The student's desired target score or goal."
        },
        "dailyAvailableStudyTimeMinutes": {
          "type": "number",
          "description": "The amount of time (in minutes) the student can dedicate to studying daily."
        },
        "preferredLanguage": {
          "type": "string",
          "description": "The student's preferred interface and content language (e.g., 'Kazakh')."
        },
        "preferredFormat": {
          "type": "string",
          "description": "The student's preferred content format (e.g., 'short' for concise, 'detailed' for in-depth explanations)."
        },
        "activityHistory": {
          "type": "array",
          "description": "List of dates (YYYY-MM-DD) when the student accessed the platform.",
          "items": {
            "type": "string"
          }
        },
        "streakDays": {
          "type": "number",
          "description": "Number of consecutive days the student has been active."
        },
        "lastVisitDate": {
          "type": "string",
          "description": "The last date the user visited the app.",
          "format": "date"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the student profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the student profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "currentScore",
        "targetScore",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/studentProfiles/{studentId}",
        "definition": {
          "entityName": "StudentProfile",
          "schema": {
            "$ref": "#/backend/entities/StudentProfile"
          },
          "description": "Stores a student's personal learning profile."
        }
      }
    ]
  }
}
